import { useCallback, useEffect, useMemo, useState } from 'react'
import './App.css'

const STORAGE_KEY = 'studiocats-manager.v1.projects'
const imagePool = ['/demo/portfolio-01.png', '/demo/portfolio-02.png', '/demo/portfolio-03.png', '/demo/portfolio-04.png', '/demo/portfolio-05.png']
const seedProjects = [
  { id: 'cloud-layer-dress', demo: true, title: 'Cloud Layer Dress', slug: 'cloud-layer-dress', category: 'Virtual Fashion', date: '2026.08.21', status: '초안', summary: 'CLO 3D에서 완성한 레이어드 드레스와 리깅 과정을 정리하는 가상 패션 프로젝트입니다.', tags: ['CLO 3D', 'Virtual Fashion', 'Render'], boothUrl: 'https://studiocats.booth.pm/', youtubeUrl: '', gallery: [{ id: 'cloud-cover', src: imagePool[0], name: 'cloud-layer-cover.png' }, { id: 'cloud-02', src: imagePool[1], name: 'cloud-layer-detail.png' }, { id: 'cloud-03', src: imagePool[2], name: 'cloud-layer-pattern.png' }, { id: 'cloud-04', src: imagePool[3], name: 'cloud-layer-render.png' }], coverId: 'cloud-cover' },
  { id: 'softform-collection', demo: true, title: 'Softform Collection', slug: 'softform-collection', category: 'Virtual Fashion', date: '2026.08.10', status: '초안', summary: '부드러운 구조감과 실루엣을 탐구한 버추얼 컬렉션입니다.', tags: ['CLO 3D', 'Unreal Engine'], boothUrl: '', youtubeUrl: '', gallery: [{ id: 'softform-cover', src: imagePool[1], name: 'softform-cover.png' }], coverId: 'softform-cover' },
  { id: 'signal-accessory-set', demo: true, title: 'Signal Accessory Set', slug: 'signal-accessory-set', category: 'Anime Style', date: '2026.07.28', status: '초안', summary: 'VRChat 아바타를 위한 액세서리 세트입니다.', tags: ['VRChat', 'Blender'], boothUrl: 'https://studiocats.booth.pm/', youtubeUrl: '', gallery: [{ id: 'signal-cover', src: imagePool[4], name: 'signal-cover.png' }], coverId: 'signal-cover' },
]

function Icon({ name, size = 18 }) {
  const paths = {
    grid: <><rect x="3" y="3" width="7" height="7" rx="1" /><rect x="14" y="3" width="7" height="7" rx="1" /><rect x="3" y="14" width="7" height="7" rx="1" /><rect x="14" y="14" width="7" height="7" rx="1" /></>,
    folder: <path d="M3 6.5h6l2 2h10v9.8A2.7 2.7 0 0 1 18.3 21H5.7A2.7 2.7 0 0 1 3 18.3z" />, cube: <path d="m12 3 8 4.5v9L12 21l-8-4.5v-9zM4.3 7.7 12 12l7.7-4.3M12 12v9" />,
    book: <><path d="M3.5 5.5A2.5 2.5 0 0 1 6 3h5.5v16H6A2.5 2.5 0 0 0 3.5 21z" /><path d="M20.5 5.5A2.5 2.5 0 0 0 18 3h-5.5v16H18a2.5 2.5 0 0 1 2.5 2z" /></>, user: <><circle cx="12" cy="7.5" r="3.5" /><path d="M4.5 21c.8-4 3.2-6 7.5-6s6.7 2 7.5 6" /></>,
    image: <><rect x="3" y="4" width="18" height="16" rx="1.5" /><circle cx="8.5" cy="9" r="1.5" /><path d="m4 18 5.5-5 3.4 3.1 2.3-2.1L20 18" /></>, settings: <><circle cx="12" cy="12" r="3" /><path d="M19.4 15a1.6 1.6 0 0 0 .3 1.8l.1.1-2.1 2.1-.1-.1a1.6 1.6 0 0 0-1.8-.3 1.6 1.6 0 0 0-1 1.4v.2h-3v-.2a1.6 1.6 0 0 0-1-1.4 1.6 1.6 0 0 0-1.8.3l-.1.1-2.1-2.1.1-.1a1.6 1.6 0 0 0 .3-1.8 1.6 1.6 0 0 0-1.4-1H5.5v-3h.2a1.6 1.6 0 0 0 1.4-1 1.6 1.6 0 0 0-.3-1.8l-.1-.1L8.8 6l.1.1a1.6 1.6 0 0 0 1.8.3 1.6 1.6 0 0 0 1-1.4v-.2h3V5a1.6 1.6 0 0 0 1 1.4 1.6 1.6 0 0 0 1.8-.3l.1-.1 2.1 2.1-.1.1a1.6 1.6 0 0 0-.3 1.8 1.6 1.6 0 0 0 1.4 1h.2v3h-.2a1.6 1.6 0 0 0-1.4 1Z" /></>,
    plus: <path d="M12 5v14M5 12h14" />, eye: <><path d="M2.5 12s3.3-5.5 9.5-5.5S21.5 12 21.5 12s-3.3 5.5-9.5 5.5S2.5 12 2.5 12Z" /><circle cx="12" cy="12" r="2.5" /></>, upload: <><path d="M12 16V3M7 8l5-5 5 5" /><path d="M4 14v5h16v-5" /></>, more: <path d="M5 12h.01M12 12h.01M19 12h.01" strokeWidth="3" strokeLinecap="round" />, star: <path d="m12 3 2.8 5.7 6.2.9-4.5 4.4 1.1 6.2-5.6-2.9-5.6 2.9 1.1-6.2L3 9.6l6.2-.9z" />, trash: <><path d="M4 7h16M9 7V4h6v3M6.5 7l.8 13h9.4l.8-13M10 11v5M14 11v5" /></>, grip: <path d="M8 6h.01M16 6h.01M8 12h.01M16 12h.01M8 18h.01M16 18h.01" strokeWidth="3" strokeLinecap="round" />, arrow: <path d="m8 5 7 7-7 7" />, close: <path d="m6 6 12 12M18 6 6 18" />,
  }
  return <svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.65" strokeLinecap="round" strokeLinejoin="round" aria-hidden="true">{paths[name]}</svg>
}

function readStoredProjects() { try { const stored = window.localStorage.getItem(STORAGE_KEY); return stored ? JSON.parse(stored) : seedProjects } catch { return seedProjects } }
function makeProject() { const stamp = Date.now(); return { id: `project-${stamp}`, title: '새 프로젝트', slug: `new-project-${stamp}`, category: 'Virtual Fashion', date: new Date().toISOString().slice(0, 10).replaceAll('-', '.'), status: '초안', summary: '', tags: [], boothUrl: '', youtubeUrl: '', gallery: [], coverId: '' } }
function readFiles(files) { return Promise.all(files.map((file) => new Promise((resolve, reject) => { const reader = new FileReader(); reader.onload = () => resolve({ name: file.name, dataUrl: reader.result }); reader.onerror = reject; reader.readAsDataURL(file) }))) }
const NAV_ITEMS = [['Dashboard', '대시보드', 'grid'], ['Portfolio', '포트폴리오', 'folder'], ['Apps', 'Apps', 'cube'], ['Journal', '저널', 'book'], ['About', '소개', 'user'], ['Media', '미디어', 'image'], ['Settings', '설정', 'settings']]

function AppsScreen({ activeNav, onNavigate, notify, toast }) {
  const [apps, setApps] = useState([])
  const [selectedId, setSelectedId] = useState('')
  const [state, setState] = useState('Apps 불러오는 중')
  const [hydrated, setHydrated] = useState(false)
  useEffect(() => {
    let cancelled = false
    fetch('/api/apps').then(async (response) => { if (!response.ok) throw new Error((await response.json()).error); return response.json() }).then((data) => { if (!cancelled) { setApps(data.apps); setSelectedId(data.apps[0]?.id ?? ''); setState('저장됨') } }).catch(() => { if (!cancelled) setState('저장 실패') }).finally(() => { if (!cancelled) setHydrated(true) })
    return () => { cancelled = true }
  }, [])
  useEffect(() => {
    if (!hydrated) return undefined
    const timer = window.setTimeout(async () => { try { setState('저장 중'); const response = await fetch('/api/apps', { method: 'PUT', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ apps }) }); if (!response.ok) throw new Error(); setState('저장됨') } catch { setState('저장 실패') } }, 700)
    return () => window.clearTimeout(timer)
  }, [apps, hydrated])
  const selected = apps.find((app) => app.id === selectedId) ?? apps[0]
  function update(patch) { setApps((current) => current.map((app) => app.id === selected.id ? { ...app, ...patch } : app)) }
  function add() { const stamp = Date.now(); const app = { id: `app-${stamp}`, title: '새 앱', slug: `new-app-${stamp}`, category: 'AI Tools', status: '초안', version: '', platform: '', summary: '', details: '', distribution: '', license: '', price: '', externalUrl: '', buttonLabel: '열기', image: '', order: apps.length * 10 + 10 }; setApps((current) => [app, ...current]); setSelectedId(app.id); notify('새 앱 항목을 만들었어.') }
  function remove() { if (!selected || !window.confirm(`“${selected.title}” 앱 항목을 삭제할까?`)) return; const next = apps.filter((app) => app.id !== selected.id); setApps(next); setSelectedId(next[0]?.id ?? ''); notify('앱 항목을 삭제했어.') }
  async function upload(event) { const file = event.target.files?.[0]; event.target.value = ''; if (!file) return; if (file.size > 5 * 1024 * 1024) return notify('이미지는 5MB 이하로 넣어줘.'); try { setState('이미지 복사 중'); const response = await fetch('/api/media', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ collection: 'apps', files: await readFiles([file]) }) }); const body = await response.json(); if (!response.ok) throw new Error(body.error); update({ image: body.images[0].src }); notify('앱 이미지를 홈페이지 폴더에 복사했어.') } catch (error) { setState('저장 실패'); notify(error.message || '이미지 추가에 실패했어.') } }
  async function generate() { if (!selected) return; const next = apps.map((app) => app.id === selected.id ? { ...app, status: '공개됨' } : app); try { const save = await fetch('/api/apps', { method: 'PUT', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ apps: next }) }); const data = await save.json(); if (!save.ok) throw new Error(data.error); setApps(data.apps); const response = await fetch('/api/generate', { method: 'POST' }); const result = await response.json(); if (!response.ok) throw new Error(result.error); setState('공개 파일 생성됨'); notify(`Apps 페이지를 포함해 ${result.generated.length}개 파일을 만들었어.`) } catch (error) { notify(error.message || 'Apps 페이지 생성에 실패했어.') } }
  return <main className="manager-shell"><aside className="sidebar"><div className="brand"><img className="brand-mark" src="/logo_img.png" alt="StudioCats" /><span>StudioCats<br /><strong>Manager</strong></span></div><nav aria-label="관리 메뉴">{NAV_ITEMS.map(([key, label, icon]) => <button className={`nav-item ${activeNav === key ? 'is-active' : ''}`} onClick={() => onNavigate(key)} key={key}><Icon name={icon} /><span>{label}</span></button>)}</nav><div className="sidebar-bottom"><span className="sync-dot" />{state}</div></aside><section className="project-list" aria-label="앱 목록"><header className="list-header"><div><h1>Apps</h1><p>총 {apps.length}개 도구</p></div><button className="outline-button" onClick={add}><Icon name="plus" />새 앱</button></header><div className="project-rows">{apps.map((app) => <button className={`project-row ${app.id === selected?.id ? 'is-selected' : ''}`} key={app.id} onClick={() => setSelectedId(app.id)}>{app.image ? <img src={app.image} alt="" /> : <span className="project-empty">APP</span>}<span className="project-row-copy"><strong>{app.title}</strong><span>{app.category}</span><span>{app.platform || '플랫폼 미정'}</span></span><span className={`status ${app.status === '공개됨' ? 'is-public' : ''}`}>{app.status}</span></button>)}</div></section>{selected ? <section className="editor" aria-label="앱 편집"><header className="topbar"><div className="draft-state"><span className="sync-dot" />{selected.status} <span>{state}</span></div><div className="top-actions"><button className="publish-button" onClick={generate}>사이트 파일 생성</button></div></header><div className="editor-scroll"><div className="editor-heading"><h2>앱 편집</h2><button className="text-danger" onClick={remove}>앱 삭제</button></div><div className="field-grid"><label>이름<input value={selected.title} onChange={(event) => update({ title: event.target.value })} /></label><label>슬러그<input value={selected.slug} onChange={(event) => update({ slug: event.target.value })} /></label><label>카테고리<select value={selected.category} onChange={(event) => update({ category: event.target.value })}><option>Windows</option><option>Blender</option><option>Unity</option><option>ComfyUI</option><option>AI Tools</option></select></label><label>버전 / 특징<input value={selected.version} onChange={(event) => update({ version: event.target.value })} /></label><label>지원 플랫폼<input value={selected.platform} onChange={(event) => update({ platform: event.target.value })} /></label><label>정렬 순서<input type="number" value={selected.order} onChange={(event) => update({ order: event.target.value })} /></label></div><label className="wide-field">소개<textarea rows="4" value={selected.summary} onChange={(event) => update({ summary: event.target.value })} /></label><div className="field-grid links"><label>배포처<input value={selected.distribution} onChange={(event) => update({ distribution: event.target.value })} placeholder="GitHub / BOOTH" /></label><label>가격 / 라이선스<input value={selected.price || selected.license} onChange={(event) => update({ price: event.target.value })} placeholder="Free / JPY 500" /></label><label>외부 링크<input value={selected.externalUrl} onChange={(event) => update({ externalUrl: event.target.value })} placeholder="https://" /></label><label>버튼 문구<input value={selected.buttonLabel} onChange={(event) => update({ buttonLabel: event.target.value })} /></label></div><label className="wide-field">보조 정보<input value={selected.details} onChange={(event) => update({ details: event.target.value })} placeholder="예: UI 한국어 / English / 日本語" /></label><section className="gallery-section"><div className="section-title"><div><h3>대표 이미지</h3><p>Apps 목록에 보이는 이미지 한 장이야.</p></div><label className="add-image"><Icon name="upload" />이미지 추가<input type="file" accept="image/png,image/jpeg,image/webp,image/gif" onChange={upload} /></label></div>{selected.image && <img className="app-hero" src={selected.image} alt="" />}</section></div></section> : <section className="editor" />}<aside className="preview-rail"><div><span className="rail-title">미리보기</span><p>Apps 목록 카드</p></div>{selected && <article className="portfolio-preview">{selected.image ? <img src={selected.image} alt="" /> : <div className="preview-empty">{selected.distribution || 'APP'}</div>}<div><h3>{selected.title}</h3><p>{selected.category} · {selected.platform}</p><span>{selected.buttonLabel || '열기'} <Icon name="arrow" size={16} /></span></div></article>}</aside>{toast && <div className="toast" role="status">{toast}</div>}</main>
}

function JournalScreen({ activeNav, onNavigate, notify, toast }) {
  const [posts, setPosts] = useState([])
  const [selectedId, setSelectedId] = useState('')
  const [state, setState] = useState('저널 불러오는 중')
  const [hydrated, setHydrated] = useState(false)
  useEffect(() => { let cancelled = false; fetch('/api/journal').then(async (response) => { if (!response.ok) throw new Error((await response.json()).error); return response.json() }).then((data) => { if (!cancelled) { setPosts(data.posts); setSelectedId(data.posts[0]?.id ?? ''); setState('저장됨') } }).catch(() => { if (!cancelled) setState('저장 실패') }).finally(() => { if (!cancelled) setHydrated(true) }); return () => { cancelled = true } }, [])
  useEffect(() => { if (!hydrated) return undefined; const timer = window.setTimeout(async () => { try { setState('저장 중'); const response = await fetch('/api/journal', { method: 'PUT', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ posts }) }); if (!response.ok) throw new Error(); setState('저장됨') } catch { setState('저장 실패') } }, 700); return () => window.clearTimeout(timer) }, [posts, hydrated])
  const selected = posts.find((post) => post.id === selectedId) ?? posts[0]
  function update(patch) { setPosts((current) => current.map((post) => post.id === selected.id ? { ...post, ...patch } : post)) }
  function add() { const stamp = Date.now(); const post = { id: `post-${stamp}`, title: '새 글', slug: `new-post-${stamp}`, status: '초안', date: new Date().toISOString().slice(0, 10).replaceAll('-', '.'), summary: '', tags: [], hero: '', protection: { mode: 'public' }, blocks: [{ id: `block-${stamp}`, type: 'text', value: '', caption: '' }] }; setPosts((current) => [post, ...current]); setSelectedId(post.id); notify('새 저널 글을 만들었어.') }
  function remove() { if (!selected || !window.confirm(`“${selected.title}” 글을 삭제할까?`)) return; const next = posts.filter((post) => post.id !== selected.id); setPosts(next); setSelectedId(next[0]?.id ?? ''); notify('저널 글을 삭제했어.') }
  function updateBlock(id, patch) { update({ blocks: selected.blocks.map((block) => block.id === id ? { ...block, ...patch } : block) }) }
  function updateProtection(patch) { update({ protection: { ...(selected.protection || { mode: 'public' }), ...patch } }) }
  function addBlock() { update({ blocks: [...selected.blocks, { id: `block-${Date.now()}`, type: 'text', value: '', caption: '' }] }) }
  function removeBlock(id) { update({ blocks: selected.blocks.filter((block) => block.id !== id) }) }
  async function uploadHero(event) { const file = event.target.files?.[0]; event.target.value = ''; if (!file) return; if (file.size > 5 * 1024 * 1024) return notify('이미지는 5MB 이하로 넣어줘.'); try { setState('이미지 복사 중'); const response = await fetch('/api/media', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ collection: 'journal', files: await readFiles([file]) }) }); const body = await response.json(); if (!response.ok) throw new Error(body.error); update({ hero: body.images[0].src }); notify('대표 이미지를 홈페이지 폴더에 복사했어.') } catch (error) { setState('저장 실패'); notify(error.message || '이미지 추가에 실패했어.') } }
  async function generate() { if (!selected) return; const next = posts.map((post) => post.id === selected.id ? { ...post, status: '공개됨' } : post); try { const save = await fetch('/api/journal', { method: 'PUT', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ posts: next }) }); const data = await save.json(); if (!save.ok) throw new Error(data.error); setPosts(data.posts); const response = await fetch('/api/generate', { method: 'POST' }); const result = await response.json(); if (!response.ok) throw new Error(result.error); setState('공개 파일 생성됨'); notify(`저널 페이지를 포함해 ${result.generated.length}개 파일을 만들었어.`) } catch (error) { notify(error.message || '저널 페이지 생성에 실패했어.') } }
  return <main className="manager-shell"><aside className="sidebar"><div className="brand"><img className="brand-mark" src="/logo_img.png" alt="StudioCats" /><span>StudioCats<br /><strong>Manager</strong></span></div><nav aria-label="관리 메뉴">{NAV_ITEMS.map(([key, label, icon]) => <button className={`nav-item ${activeNav === key ? 'is-active' : ''}`} onClick={() => onNavigate(key)} key={key}><Icon name={icon} /><span>{label}</span></button>)}</nav><div className="sidebar-bottom"><span className="sync-dot" />{state}</div></aside><section className="project-list" aria-label="저널 목록"><header className="list-header"><div><h1>저널</h1><p>총 {posts.length}개 글</p></div><button className="outline-button" onClick={add}><Icon name="plus" />새 글</button></header><div className="project-rows">{posts.map((post) => <button className={`project-row ${post.id === selected?.id ? 'is-selected' : ''}`} key={post.id} onClick={() => setSelectedId(post.id)}>{post.hero ? <img src={post.hero} alt="" /> : <span className="project-empty">NOTE</span>}<span className="project-row-copy"><strong>{post.title}</strong><span>{post.date}</span><span>{post.tags?.join(' · ') || '태그 없음'}</span></span><span className={`status ${post.status === '공개됨' ? 'is-public' : ''}`}>{post.status}</span></button>)}</div></section>{selected ? <section className="editor" aria-label="저널 글 편집"><header className="topbar"><div className="draft-state"><span className="sync-dot" />{selected.status} <span>{state}</span></div><div className="top-actions"><button className="publish-button" onClick={generate}>사이트 파일 생성</button></div></header><div className="editor-scroll"><div className="editor-heading"><h2>저널 글 편집</h2><button className="text-danger" onClick={remove}>글 삭제</button></div><div className="field-grid"><label>제목<input value={selected.title} onChange={(event) => update({ title: event.target.value })} /></label><label>슬러그<input value={selected.slug} onChange={(event) => update({ slug: event.target.value })} /></label><label>작성일<input value={selected.date} onChange={(event) => update({ date: event.target.value })} /></label><label>태그<input value={selected.tags.join(', ')} onChange={(event) => update({ tags: event.target.value.split(',').map((tag) => tag.trim()).filter(Boolean) })} placeholder="예: Blender, Workflow" /></label></div><section className="gallery-section"><div className="section-title"><div><h3>열람 설정</h3><p>공개 글은 바로 열리고, 비밀번호 글은 학생에게 입력창이 보여.</p></div></div><div className="field-grid"><label>열람 방식<select value={selected.protection?.mode || 'public'} onChange={(event) => updateProtection({ mode: event.target.value })}><option value="public">공개</option><option value="password">비밀번호 입력</option></select></label>{selected.protection?.mode === 'password' && <label>글 비밀번호<input type="password" value={selected.protection.password || ''} onChange={(event) => updateProtection({ password: event.target.value })} placeholder={selected.protection.passwordHash ? '변경할 때만 새 비밀번호 입력' : '학생에게 알려줄 비밀번호'} /></label>}</div>{selected.protection?.mode === 'password' && <p className="field-hint">이건 강한 보안이 아니라 글을 열기 전 비밀번호를 묻는 용도야. 비밀번호는 저장할 때 암호화된 값으로만 남아.</p>}</section><label className="wide-field">요약<textarea rows="3" value={selected.summary} onChange={(event) => update({ summary: event.target.value })} /></label><section className="gallery-section"><div className="section-title"><div><h3>대표 이미지</h3><p>목록과 글 상단에 보이는 이미지야.</p></div><label className="add-image"><Icon name="upload" />이미지 추가<input type="file" accept="image/png,image/jpeg,image/webp,image/gif" onChange={uploadHero} /></label></div>{selected.hero && <img className="app-hero" src={selected.hero} alt="" />}</section><section className="gallery-section"><div className="section-title"><div><h3>본문 블록</h3><p>문단, 제목, 이미지, YouTube, 링크, 코드를 순서대로 쌓아.</p></div><button className="outline-button" onClick={addBlock}><Icon name="plus" />블록 추가</button></div><div className="blocks">{selected.blocks.map((block, index) => <article className="content-block" key={block.id}><div className="block-head"><strong>{index + 1}</strong><select value={block.type} onChange={(event) => updateBlock(block.id, { type: event.target.value })}><option value="text">문단</option><option value="heading">소제목</option><option value="image">이미지 경로</option><option value="youtube">YouTube</option><option value="link">외부 링크</option><option value="code">코드</option></select><button className="text-danger" onClick={() => removeBlock(block.id)}>삭제</button></div><textarea rows={block.type === 'code' ? 7 : 4} value={block.value} onChange={(event) => updateBlock(block.id, { value: event.target.value })} placeholder={block.type === 'image' ? '/uploads/journal/...' : block.type === 'youtube' ? 'https://youtu.be/...' : block.type === 'link' ? 'https://' : '내용을 입력해줘.'} /><input value={block.caption || ''} onChange={(event) => updateBlock(block.id, { caption: event.target.value })} placeholder="이미지 캡션 또는 링크 문구 (선택)" /></article>)}</div></section></div></section> : <section className="editor" />}<aside className="preview-rail"><div><span className="rail-title">미리보기</span><p>저널 목록 카드</p></div>{selected && <article className="portfolio-preview">{selected.hero ? <img src={selected.hero} alt="" /> : <div className="preview-empty">JOURNAL</div>}<div><h3>{selected.title}</h3><p>{selected.date}</p><span>{selected.protection?.mode === 'password' ? '비밀번호 필요' : '읽기'} <Icon name="arrow" size={16} /></span></div></article>}</aside>{toast && <div className="toast" role="status">{toast}</div>}</main>
}

function SimpleShell({ activeNav, onNavigate, state, children, toast }) {
  return <main className="manager-shell"><aside className="sidebar"><div className="brand"><img className="brand-mark" src="/logo_img.png" alt="StudioCats" /><span>StudioCats<br /><strong>Manager</strong></span></div><nav aria-label="관리 메뉴">{NAV_ITEMS.map(([key, label, icon]) => <button className={`nav-item ${activeNav === key ? 'is-active' : ''}`} onClick={() => onNavigate(key)} key={key}><Icon name={icon} /><span>{label}</span></button>)}</nav><div className="sidebar-bottom"><span className="sync-dot" />{state}</div></aside>{children}{toast && <div className="toast" role="status">{toast}</div>}</main>
}

function DashboardScreen({ activeNav, onNavigate, notify, toast }) {
  const [counts, setCounts] = useState(null)
  const [publishing, setPublishing] = useState(false)
  useEffect(() => { Promise.all(['/api/portfolio', '/api/apps', '/api/journal', '/api/site'].map((url) => fetch(url).then((response) => response.json()))).then(([portfolio, apps, journal, site]) => setCounts({ projects: portfolio.projects.length, apps: apps.apps.length, posts: journal.posts.length, domain: site.domain })).catch(() => setCounts({ projects: 0, apps: 0, posts: 0, domain: '연결 실패' })) }, [])
  const cards = counts ? [['포트폴리오', counts.projects, 'Portfolio'], ['Apps', counts.apps, 'Apps'], ['저널', counts.posts, 'Journal']] : []
  async function publish() { if (!window.confirm('생성된 콘텐츠 파일을 현재 브랜치에 커밋하고 GitHub로 push할까?')) return; setPublishing(true); try { const response = await fetch('/api/publish', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ message: 'Publish StudioCats content' }) }); const data = await response.json(); if (!response.ok) throw new Error(data.error); notify(data.committed ? `${data.branch}에 커밋하고 push했어.` : data.message) } catch (error) { notify(error.message || 'GitHub 반영에 실패했어.') } finally { setPublishing(false) } }
  return <SimpleShell activeNav={activeNav} onNavigate={onNavigate} state="저장소 연결됨" toast={toast}><section className="dashboard"><div className="dash-head"><div><span className="rail-title">STUDIOCATS MANAGER</span><h1>콘텐츠 현황</h1><p>{counts?.domain || '저장소 연결 중'}</p></div></div><div className="dash-cards">{cards.map(([label, count, target]) => <button className="dash-card" key={target} onClick={() => onNavigate(target)}><span>{label}</span><strong>{count}</strong><small>관리하기 →</small></button>)}</div><section className="dash-guide"><h2>공개 흐름</h2><p>내용 저장 → 사이트 파일 생성 → GitHub에 커밋 및 push. 마지막 단계 전에는 공개 사이트가 바뀌지 않아.</p><button className="publish-button" disabled={publishing} onClick={publish}>{publishing ? 'GitHub 반영 중…' : 'GitHub에 전체 반영'}</button></section></section><aside className="preview-rail"><span className="rail-title">바로가기</span><button className="outline-button dash-action" onClick={() => onNavigate('About')}>소개 / 연락처</button><button className="outline-button dash-action" onClick={() => onNavigate('Media')}>미디어 보관함</button><button className="outline-button dash-action" onClick={() => onNavigate('Settings')}>사이트 설정</button></aside></SimpleShell>
}

function SiteScreen({ activeNav, onNavigate, notify, toast, mode }) {
  const [site, setSite] = useState(null); const [state, setState] = useState('사이트 정보 불러오는 중'); const [hydrated, setHydrated] = useState(false)
  useEffect(() => { fetch('/api/site').then((response) => response.json()).then((data) => { setSite(data); setState('저장됨') }).catch(() => setState('저장 실패')).finally(() => setHydrated(true)) }, [])
  useEffect(() => { if (!hydrated || !site) return undefined; const timer = window.setTimeout(async () => { try { setState('저장 중'); const response = await fetch('/api/site', { method: 'PUT', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(site) }); const data = await response.json(); if (!response.ok) throw new Error(data.error); setSite(data); setState('저장됨') } catch { setState('저장 실패') } }, 700); return () => window.clearTimeout(timer) }, [site, hydrated])
  function update(patch) { setSite((current) => ({ ...current, ...patch })) }
  function lines(key) { return (site?.[key] || []).join('\n') }
  if (!site) return <SimpleShell activeNav={activeNav} onNavigate={onNavigate} state={state} toast={toast}><section className="editor" /></SimpleShell>
  const isSettings = mode === 'settings'
  return <SimpleShell activeNav={activeNav} onNavigate={onNavigate} state={state} toast={toast}><section className="project-list"><header className="list-header"><div><h1>{isSettings ? '설정' : '소개'}</h1><p>{isSettings ? '사이트 기본 정보' : 'About 페이지 내용'}</p></div></header><div className="field-hint">자동 저장 중이고, 공개할 때 정적 About 페이지가 다시 만들어져.</div></section><section className="editor"><header className="topbar"><div className="draft-state"><span className="sync-dot" />{state}</div><div className="top-actions"><button className="publish-button" onClick={async () => { const response = await fetch('/api/generate', { method: 'POST' }); const data = await response.json(); notify(response.ok ? `About 포함 ${data.generated.length}개 파일을 만들었어.` : data.error) }}>사이트 파일 생성</button></div></header><div className="editor-scroll">{isSettings ? <><div className="editor-heading"><h2>기본 정보</h2></div><div className="field-grid"><label>브랜드 이름<input value={site.brand} onChange={(event) => update({ brand: event.target.value })} /></label><label>사이트 주소<input value={site.domain} onChange={(event) => update({ domain: event.target.value })} /></label><label>문의 이메일<input value={site.email} onChange={(event) => update({ email: event.target.value })} /></label></div></> : <><div className="editor-heading"><h2>소개와 연락처</h2></div><label className="wide-field">소개 제목<textarea rows="3" value={site.aboutTitle} onChange={(event) => update({ aboutTitle: event.target.value })} /></label><label className="wide-field">소개글<textarea rows="6" value={site.aboutIntro} onChange={(event) => update({ aboutIntro: event.target.value })} /></label><label className="wide-field">문의 안내<textarea rows="3" value={site.contactText} onChange={(event) => update({ contactText: event.target.value })} /></label><label className="wide-field">경력 (한 줄에 하나)<textarea rows="6" value={lines('career')} onChange={(event) => update({ career: event.target.value.split('\n').map((value) => value.trim()).filter(Boolean) })} /></label><label className="wide-field">작업 분야 (한 줄에 하나)<textarea rows="6" value={lines('services')} onChange={(event) => update({ services: event.target.value.split('\n').map((value) => value.trim()).filter(Boolean) })} /></label><label className="wide-field">외부 링크 (한 줄에 `이름 | URL`)<textarea rows="6" value={site.links.map((link) => `${link.label} | ${link.url}`).join('\n')} onChange={(event) => update({ links: event.target.value.split('\n').map((line) => { const [label, url] = line.split('|').map((value) => value.trim()); return { label, url } }).filter((link) => link.label && link.url) })} /></label></>}</div></section><aside className="preview-rail"><span className="rail-title">미리보기</span><article className="portfolio-preview"><div><h3>{site.brand}</h3><p>{site.email}</p><span>{site.domain}</span></div></article></aside></SimpleShell>
}

function MediaScreen({ activeNav, onNavigate, notify, toast }) {
  const [collection, setCollection] = useState('portfolio'); const [images, setImages] = useState([]); const [state, setState] = useState('미디어 불러오는 중')
  const refresh = useCallback(() => fetch(`/api/media?collection=${collection}`).then((response) => response.json()).then((data) => { setImages(data.images); setState(`${data.images.length}개 이미지`) }).catch(() => setState('불러오기 실패')), [collection])
  useEffect(() => { refresh() }, [refresh])
  async function upload(event) { const files = [...event.target.files]; event.target.value = ''; if (!files.length) return; try { setState('이미지 복사 중'); const response = await fetch('/api/media', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ collection, files: await readFiles(files) }) }); const data = await response.json(); if (!response.ok) throw new Error(data.error); notify(`${data.images.length}장을 추가했어.`); refresh() } catch (error) { notify(error.message || '이미지 추가에 실패했어.') } }
  return <SimpleShell activeNav={activeNav} onNavigate={onNavigate} state={state} toast={toast}><section className="media-screen"><header className="list-header"><div><h1>미디어</h1><p>사이트에 복사된 이미지 보관함</p></div><label className="add-image"><Icon name="upload" />이미지 추가<input type="file" accept="image/png,image/jpeg,image/webp,image/gif" multiple onChange={upload} /></label></header><div className="media-tabs">{['portfolio', 'apps', 'journal'].map((item) => <button className={collection === item ? 'chip on' : 'chip'} onClick={() => setCollection(item)} key={item}>{item}</button>)}</div><div className="media-library">{images.map((image) => <article key={image.src}><img src={image.src} alt="" /><span>{image.name}</span></article>)}</div></section><aside className="preview-rail"><span className="rail-title">안내</span><p className="field-hint">이미지를 삭제하면 현재 프로젝트·앱·글에서 참조 중인지 확인하기 어려워서, 보관함에서는 삭제하지 않게 뒀어.</p></aside></SimpleShell>
}

function App() {
  const [projects, setProjects] = useState(readStoredProjects)
  const [selectedId, setSelectedId] = useState(() => readStoredProjects()[0]?.id ?? '')
  const [activeNav, setActiveNav] = useState('Portfolio')
  const [tagDraft, setTagDraft] = useState('')
  const [toast, setToast] = useState('')
  const [isPreviewOpen, setIsPreviewOpen] = useState(false)
  const [isPublishOpen, setIsPublishOpen] = useState(false)
  const [draggedImageId, setDraggedImageId] = useState(null)
  const [repositoryState, setRepositoryState] = useState('저장소 연결 중')
  const [isHydrated, setIsHydrated] = useState(false)
  const [isPublishing, setIsPublishing] = useState(false)
  const [publishResult, setPublishResult] = useState(null)

  useEffect(() => {
    let cancelled = false
    fetch('/api/portfolio').then(async (response) => { if (!response.ok) throw new Error((await response.json()).error); return response.json() }).then((data) => {
      if (cancelled || !data.projects?.length) return
      setProjects(data.projects); setSelectedId((current) => data.projects.some((project) => project.id === current) ? current : data.projects[0].id); setRepositoryState('저장됨')
    }).catch(() => { if (!cancelled) { setRepositoryState('브라우저 임시 저장'); setToast('저장소 연결이 안 돼서 이 브라우저에만 임시 저장 중이야.') } }).finally(() => { if (!cancelled) setIsHydrated(true) })
    return () => { cancelled = true }
  }, [])
  useEffect(() => { window.localStorage.setItem(STORAGE_KEY, JSON.stringify(projects)) }, [projects])
  useEffect(() => { if (!toast) return undefined; const timer = window.setTimeout(() => setToast(''), 3000); return () => window.clearTimeout(timer) }, [toast])
  useEffect(() => {
    if (!isHydrated) return undefined
    const timer = window.setTimeout(async () => {
      setRepositoryState('저장 중')
      try { const response = await fetch('/api/portfolio', { method: 'PUT', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ projects }) }); if (!response.ok) throw new Error((await response.json()).error); setRepositoryState('저장됨') } catch { setRepositoryState('브라우저 임시 저장') }
    }, 700)
    return () => window.clearTimeout(timer)
  }, [projects, isHydrated])

  const selected = useMemo(() => projects.find((project) => project.id === selectedId) ?? projects[0], [projects, selectedId])
  const cover = selected?.gallery?.find((image) => image.id === selected.coverId) ?? selected?.gallery?.[0]
  function updateProject(patch) { setProjects((current) => current.map((project) => (project.id === selected.id ? { ...project, ...patch } : project))) }
  function addProject() { const project = makeProject(); setProjects((current) => [project, ...current]); setSelectedId(project.id); setToast('새 프로젝트를 만들었어. 먼저 이미지를 넣어줘.') }
  function deleteProject() { if (projects.length === 1 || !window.confirm(`“${selected.title}” 프로젝트를 삭제할까?`)) return; const next = projects.filter((project) => project.id !== selected.id); setProjects(next); setSelectedId(next[0].id); setToast('프로젝트를 삭제했어.') }
  function addTag() { const tag = tagDraft.trim(); if (!tag || selected.tags.includes(tag)) return; updateProject({ tags: [...selected.tags, tag] }); setTagDraft('') }
  function removeTag(tag) { updateProject({ tags: selected.tags.filter((item) => item !== tag) }) }
  async function addImages(event) {
    const files = [...event.target.files]; event.target.value = ''
    if (!files.length) return
    if (files.some((file) => file.size > 5 * 1024 * 1024)) return setToast('이미지 한 장은 5MB 이하로 넣어줘.')
    try {
      setRepositoryState('이미지 복사 중')
      const response = await fetch('/api/media', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ files: await readFiles(files) }) })
      const body = await response.json(); if (!response.ok) throw new Error(body.error)
      const gallery = [...selected.gallery, ...body.images]; updateProject({ gallery, coverId: selected.coverId || body.images[0].id }); setRepositoryState('저장 대기'); setToast(`${body.images.length}장을 홈페이지 폴더에 복사했어.`)
    } catch (error) { setRepositoryState('저장 실패'); setToast(error.message || '이미지 추가에 실패했어.') }
  }
  function removeImage(imageId) { const gallery = selected.gallery.filter((image) => image.id !== imageId); updateProject({ gallery, coverId: selected.coverId === imageId ? (gallery[0]?.id ?? '') : selected.coverId }); setToast('이미지를 목록에서 뺐어. 업로드 파일은 안전하게 남겨뒀어.') }
  function reorderImages(overImageId) { if (!draggedImageId || draggedImageId === overImageId) return; const from = selected.gallery.findIndex((image) => image.id === draggedImageId); const to = selected.gallery.findIndex((image) => image.id === overImageId); const gallery = [...selected.gallery]; const [moved] = gallery.splice(from, 1); gallery.splice(to, 0, moved); updateProject({ gallery }); setDraggedImageId(null) }
  function preparePublish() { if (selected.demo) return setToast('샘플 항목은 공개하지 않아. 새 프로젝트로 실제 작업을 만들어줘.'); if (!selected.gallery.length) return setToast('공개하려면 이미지를 최소 한 장 넣어줘.'); setPublishResult(null); setIsPublishOpen(true) }
  async function publishProject() {
    const next = projects.map((project) => project.id === selected.id ? { ...project, status: '공개됨' } : project)
    setIsPublishing(true); setRepositoryState('공개 파일 생성 중')
    try {
      const saveResponse = await fetch('/api/portfolio', { method: 'PUT', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ projects: next }) }); const saveBody = await saveResponse.json(); if (!saveResponse.ok) throw new Error(saveBody.error)
      setProjects(saveBody.projects)
      const response = await fetch('/api/generate', { method: 'POST' }); const body = await response.json(); if (!response.ok) throw new Error(body.error)
      setRepositoryState('공개 파일 생성됨'); setPublishResult(body); setToast(`사이트 파일 ${body.generated.length}개를 만들었어. GitHub 반영은 한 번 더 확인하고 눌러줘.`)
    } catch (error) { setRepositoryState('저장 실패'); setToast(error.message || '공개 파일 생성에 실패했어.') } finally { setIsPublishing(false) }
  }
  async function publishToGitHub() {
    setIsPublishing(true); setRepositoryState('GitHub 반영 중')
    try {
      const response = await fetch('/api/publish', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ message: `Publish portfolio: ${selected.title}` }) })
      const body = await response.json(); if (!response.ok) throw new Error(body.error)
      setRepositoryState(body.committed ? `${body.branch}에 반영됨` : '반영할 변경 없음'); setIsPublishOpen(false); setToast(body.committed ? `${body.branch} 브랜치에 커밋하고 push했어.` : body.message)
    } catch (error) { setRepositoryState('GitHub 반영 실패'); setToast(error.message || 'GitHub 반영에 실패했어.') } finally { setIsPublishing(false) }
  }

  const navigate = (key) => setActiveNav(key)
  if (activeNav === 'Dashboard') return <DashboardScreen activeNav={activeNav} onNavigate={navigate} notify={setToast} toast={toast} />
  if (activeNav === 'Apps') return <AppsScreen activeNav={activeNav} onNavigate={navigate} notify={setToast} toast={toast} />
  if (activeNav === 'Journal') return <JournalScreen activeNav={activeNav} onNavigate={navigate} notify={setToast} toast={toast} />
  if (activeNav === 'About') return <SiteScreen activeNav={activeNav} onNavigate={navigate} notify={setToast} toast={toast} mode="about" />
  if (activeNav === 'Media') return <MediaScreen activeNav={activeNav} onNavigate={navigate} notify={setToast} toast={toast} />
  if (activeNav === 'Settings') return <SiteScreen activeNav={activeNav} onNavigate={navigate} notify={setToast} toast={toast} mode="settings" />
  if (!selected) return <main className="manager-shell"><p className="toast">프로젝트를 불러오는 중이야.</p></main>
  return <main className="manager-shell">
    <aside className="sidebar"><div className="brand"><img className="brand-mark" src="/logo_img.png" alt="StudioCats" /><span>StudioCats<br /><strong>Manager</strong></span></div><nav aria-label="관리 메뉴">{NAV_ITEMS.map(([key, label, icon]) => <button className={`nav-item ${activeNav === key ? 'is-active' : ''}`} onClick={() => setActiveNav(key)} key={key}><Icon name={icon} /><span>{label}</span></button>)}</nav><div className="sidebar-bottom"><span className="sync-dot" />{repositoryState}</div></aside>
    <section className="project-list" aria-label="포트폴리오 목록"><header className="list-header"><div><h1>포트폴리오</h1><p>총 {projects.length}개 프로젝트</p></div><button className="outline-button" onClick={addProject}><Icon name="plus" />새 프로젝트</button></header><div className="project-rows">{projects.map((project) => { const projectCover = project.gallery.find((image) => image.id === project.coverId) ?? project.gallery[0]; return <button className={`project-row ${project.id === selected.id ? 'is-selected' : ''}`} key={project.id} onClick={() => setSelectedId(project.id)}>{projectCover ? <img src={projectCover.src} alt="" /> : <span className="project-empty">이미지 없음</span>}<span className="project-row-copy"><strong>{project.title}</strong><span>{project.category}</span><span>{project.date}</span></span><span className={`status ${project.status === '공개됨' ? 'is-public' : ''}`}>{project.status}</span></button> })}</div></section>
    <section className="editor" aria-label="프로젝트 편집"><header className="topbar"><div className="draft-state"><span className="sync-dot" />{selected.status} <span>{repositoryState}</span></div><div className="top-actions"><button className="preview-button" onClick={() => setIsPreviewOpen(true)} disabled={!cover}><Icon name="eye" />미리보기</button><button className="publish-button" onClick={preparePublish}>공개하기</button><button className="icon-button" aria-label="더 보기"><Icon name="more" /></button></div></header><div className="editor-scroll"><div className="editor-heading"><div><h2>프로젝트 편집</h2>{selected.demo && <p className="field-hint">지금 보이는 건 UI 예시야. 실제 공개는 새 프로젝트로 등록하면 돼.</p>}</div><button className="text-danger" onClick={deleteProject}>프로젝트 삭제</button></div><div className="field-grid"><label>제목<input value={selected.title} onChange={(event) => updateProject({ title: event.target.value })} /></label><label>슬러그<input value={selected.slug} onChange={(event) => updateProject({ slug: event.target.value })} /></label><label>카테고리<select value={selected.category} onChange={(event) => updateProject({ category: event.target.value })}><option>Virtual Fashion</option><option>Anime Style</option><option>3D Works</option></select></label><label>제작일<input value={selected.date} onChange={(event) => updateProject({ date: event.target.value })} /></label></div><label className="wide-field">설명<textarea value={selected.summary} onChange={(event) => updateProject({ summary: event.target.value })} placeholder="프로젝트를 소개하는 짧은 글을 써줘." rows="4" /></label><div className="wide-field"><span className="field-label">태그</span><div className="tag-box">{selected.tags.map((tag) => <button key={tag} className="tag" onClick={() => removeTag(tag)}>{tag}<Icon name="close" size={13} /></button>)}<input value={tagDraft} onChange={(event) => setTagDraft(event.target.value)} onKeyDown={(event) => { if (event.key === 'Enter') { event.preventDefault(); addTag() } }} onBlur={addTag} placeholder="태그 입력 후 Enter" /></div></div><div className="field-grid links"><label>BOOTH / 외부 링크<input value={selected.boothUrl} onChange={(event) => updateProject({ boothUrl: event.target.value })} placeholder="https://" /></label><label>YouTube 링크<input value={selected.youtubeUrl} onChange={(event) => updateProject({ youtubeUrl: event.target.value })} placeholder="https://youtu.be/..." /></label></div><section className="gallery-section"><div className="section-title"><div><h3>이미지 갤러리</h3><p>이미지를 홈페이지 폴더에 복사해두고, 드래그로 순서를 바꿔. 별표가 대표 썸네일이야.</p></div><label className="add-image"><Icon name="upload" />이미지 추가<input type="file" accept="image/png,image/jpeg,image/webp,image/gif" multiple onChange={addImages} /></label></div>{selected.gallery.length ? <div className="gallery-grid">{selected.gallery.map((image, index) => <article className={`media-tile ${selected.coverId === image.id ? 'is-cover' : ''}`} draggable onDragStart={() => setDraggedImageId(image.id)} onDragOver={(event) => event.preventDefault()} onDrop={() => reorderImages(image.id)} key={image.id}><div className="tile-actions"><span className="tile-order"><Icon name="grip" size={15} />{index + 1}</span><button aria-label="대표 썸네일로 지정" className={selected.coverId === image.id ? 'star is-active' : 'star'} onClick={() => { updateProject({ coverId: image.id }); setToast('대표 썸네일을 바꿨어.') }}><Icon name="star" size={17} /></button><button aria-label="이미지 삭제" className="tile-delete" onClick={() => removeImage(image.id)}><Icon name="trash" size={16} /></button></div><img src={image.src} alt={image.name} />{selected.coverId === image.id && <span className="cover-label">대표 썸네일</span>}</article>)}</div> : <p className="field-hint">아직 이미지가 없어. “이미지 추가”로 시작하면 돼.</p>}</section></div></section>
    <aside className="preview-rail"><div><span className="rail-title">미리보기</span><p>포트폴리오 카드 미리보기</p></div>{cover ? <article className="portfolio-preview"><img src={cover.src} alt="" /><div><h3>{selected.title || '제목 없음'}</h3><p>{selected.category}</p><span>자세히 보기 <Icon name="arrow" size={16} /></span></div></article> : <p className="field-hint">대표 이미지를 넣으면 여기서 카드가 보여.</p>}{selected.youtubeUrl && <a className="video-note" href={selected.youtubeUrl} target="_blank" rel="noreferrer">YouTube 영상 연결됨 <Icon name="arrow" size={15} /></a>}</aside>
    {toast && <div className="toast" role="status">{toast}</div>}
    {isPreviewOpen && cover && <div className="modal-backdrop" role="presentation" onMouseDown={() => setIsPreviewOpen(false)}><section className="modal preview-modal" role="dialog" aria-modal="true" aria-label="포트폴리오 미리보기" onMouseDown={(event) => event.stopPropagation()}><button className="modal-close" onClick={() => setIsPreviewOpen(false)} aria-label="미리보기 닫기"><Icon name="close" /></button><span className="modal-title">사이트 카드 미리보기</span><article className="site-card"><img src={cover.src} alt="" /><div><span>{selected.category}</span><h2>{selected.title || '제목 없음'}</h2><p>{selected.summary || '프로젝트 설명이 여기에 표시돼.'}</p>{selected.youtubeUrl && <small>▶ YouTube 영상 포함</small>}</div></article></section></div>}
    {isPublishOpen && <div className="modal-backdrop" role="presentation" onMouseDown={() => !isPublishing && setIsPublishOpen(false)}><section className="modal publish-modal" role="dialog" aria-modal="true" aria-label="사이트 공개" onMouseDown={(event) => event.stopPropagation()}><button className="modal-close" onClick={() => !isPublishing && setIsPublishOpen(false)} aria-label="닫기"><Icon name="close" /></button>{publishResult ? <><span className="modal-title">파일 생성 완료</span><h2>{publishResult.generated.length}개 파일을 확인했어.</h2><p>아래 버튼은 포트폴리오 데이터·이미지·생성된 페이지 파일만 커밋하고 현재 브랜치에 push해. 다른 작업 파일은 건드리지 않아.</p><button className="publish-button" disabled={isPublishing} onClick={publishToGitHub}>{isPublishing ? 'GitHub 반영 중…' : 'GitHub에 커밋 및 push'}</button></> : <><span className="modal-title">공개할 준비 됐어?</span><h2>{selected.title}의 정적 사이트 파일을 만들게.</h2><p>먼저 프로젝트 HTML, 카테고리 목록, 사이트맵만 생성해서 변경점을 확인할 수 있어. GitHub push는 다음 확인 단계에서만 일어나.</p><button className="publish-button" disabled={isPublishing} onClick={publishProject}>{isPublishing ? '생성 중…' : '사이트 파일 생성'}</button></>}</section></div>}
  </main>
}

export default App
